const command = require('command');

module.exports = command('writefile')
	.arguments('<path> [mode]')
	.action(function ({_: [path, mode], $stdin}) {
		#realpath(path) |> #then(([realpath]) => {
			$stdin.pipe(
				fs.createWriteStream(realpath, {flags: mode || 'w'})
			);
		});
	});
