const command = require('command');

module.exports = command('cd')
	.arguments('<path>')
	.action(({_: [path]}, push) => {
		#realpath(path).toPromise()
			.then(function ([abspath]) {
				$cwd = abspath;
			});

		push(null);
	});
