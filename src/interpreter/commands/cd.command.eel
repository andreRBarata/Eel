const command = require('command');

module.exports = command('cd')
	.arguments('<path>')
	.action(({_: [path]}, push) => {
		#realpath(path) |> #then(([abspath]) => {
			$cwd = abspath;
		});

		push(null);
	});
