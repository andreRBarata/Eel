const command	= require('command');
const path		= require('path');
const os		= require('os');

module.exports = command('realpath')
	.arguments('<location>')
	.action(({_: [location]}, push) => {
		push(path.resolve(process.cwd(), location.replace('~',
			os.homedir()
		)));

		push(null);
	});
