const marked	= require('marked');
const command	= require('command');

module.exports = command('marked')
	.arguments('<markdown> [options]')
	.action(({$stdout, _: [markdown, options]}) => {
		marked(markdown, options) |> $stdout;
	});
